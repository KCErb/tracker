<div class="modal" id="shared-modal" tabindex="-1" role="dialog" aria-labelledby="modal-header" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <!-- Close Button -->
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h4 class="modal-title" id="modal-header"></h4>
      </div>
      <!-- Body -->
      <div class="modal-body" id="myModalBody">
        <div class='row'>
          <!-- Comments column -->
          <div class='col-md-8' style="border-right: 1px solid #ccc;">
            <%= render partial: 'shared/timeline', locals: {household: household} if defined? household %>
            <%= render partial: 'shared/timeline', locals: {member: member} if defined? member %>
          </div>
          <!-- Member info and Tags column -->
          <div class='col-md-4'>
            <hr class = "hidden-lg hidden-md">
            <h4 class="modal-section-title"><i class='fa fa-book'></i> Info</h4>
            <%= render partial: 'shared/info_box' %>
            <br>
            <hr class = "hidden-lg hidden-md">
            <h4 class="modal-section-title"><i class='fa fa-tag'></i> Tags</h4>
            <%= render partial: 'shared/tags_editor', locals: {household: household} if defined? household %>
            <%= render partial: 'shared/tags_editor', locals: {member: member} if defined? member %>
          </div>

        </div>

      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>
